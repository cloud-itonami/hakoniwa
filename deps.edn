{:deps {
        ;; kotoba.bytes replaced UTF-8 .getBytes / String. here (2026-09-24). An explicit
        ;; floor: tools.deps takes the NEWEST sha it is shown.
        io.github.kotoba-lang/bytes {:git/sha "d5259f35ab7c4f5a66e63ca29ed4dcf7ba3f1ac5"}
        ;; kotoba.io replaced clojure.java.io here (2026-09-24). An explicit floor:
        ;; kotoba.io carries clojure.java.io's file/as-file/as-relative-path/
        ;; make-parents/delete-file from 98045c7, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/io {:git/sha "1b60ca825c1e705bd195f857ca4549133653a9f3"}
        ;; kotoba.lang.coll replaced clojure.set here (2026-09-24). An explicit
        ;; floor: kotoba.lang.coll carries clojure.set's union/intersection/
        ;; difference/map-invert from 0ffd5f6, and tools.deps takes the NEWEST
        ;; sha it is shown.
        io.github.kotoba-lang/coll {:git/sha "f74e248f0a07e8ae572885f50fab6bdb39dc24be"}
        ;; kotoba.lang.text, not clojure.string. Pinned as an explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, so without a floor here
        ;; this repo silently rides whatever a sibling happens to name.
        io.github.kotoba-lang/text {:git/sha "73bdb13ae7a3d004b44bca08be03a3191157a38f"}
       ;; kotoba.test (clojure.test-compatible surface), not clojure.test --
        ;; scripts/migrate-clojure-test-to-kotoba-test.cljk. Explicit floor.
        io.github.kotoba-lang/test {:git/url "https://github.com/kotoba-lang/test.git"
                                    :git/sha "2ab4885f99fd25552d380d0f6b0b54c5a72eb7ae"}
        ;; kotoba.lang.edn replaced clojure.edn here (2026-09-23). An explicit floor:
        ;; tools.deps takes the NEWEST sha it is shown, and kbb resolves git deps
        ;; only from nbb.edn, so every project file this repo has names it.
        io.github.kotoba-lang/edn {:git/sha "79dba32c8892058842c57783f06a2dfe4601aeb0"}}
 :project {:name "hakoniwa" :version "0.1.0" :glyph "箱庭" :adr "2606111500" :tier "B" :status "R1-live-operation-and-social-emission-authorized" :description "Forward-simulation observatory — the charter-clean inversion of a swarm-intelligence prediction engine (the 666ghj/MiroFish shape). Runs a CONTAINED miniature world (箱庭) of FICTIONAL latent personas forward via Friedkin-Johnsen dynamics to surface a DISTRIBUTION over possible futures, routed to resilience & preparedness; feeds mitooshi 見通し proper-scoring. R1 (founder-authorized, Council Lv7+ 1/1): runs AUTONOMOUSLY (autorun heartbeat → content-addressed append-only kotoba Datom log) with authorized SOCIAL EMISSION. Never a point prediction (G2), never a real-person model (G1), never a steering campaign (G3)."} :dependencies {} :substrate {:state "kotoba Datom log (ADR-2605312345)" :schema "schema/hakoniwa-scenario-ontology.kotoba.edn" :inference "Murakumo-only (ADR-2605215000); swarm rides baien-edge (ADR-2605242600/2605241900)" :output "mitooshi-shaped :forecast/kind :distribution record (ADR-2606051800); distribution-only (G2)" :runtime "kotoba pywasm actor (componentize-py); deterministic kernel in-WASM, LLM-persona variant operator/mesh-side"} :gates {:g1_synthetic_only true :g2_distribution_only true :g3_non_steering true :g5_murakumo_only true :g7_member_signed true :g8_r1_authorized "founder/Council Lv7+ 1/1 (2026-06-11): autonomous run + social emission AUTHORIZED; external AT-Proto relay needs operator transport; real-entity ingest + live swarm still gated"} :cells {:world "methods/world.py" :simulate "methods/simulate.py" :distribution "methods/distribution.py" :datom_emit "methods/datom_emit.py" :murakumo "methods/murakumo.py" :social "methods/social.py" :kotoba "methods/kotoba.py" :autorun "methods/autorun.py" :ingest "methods/ingest.py" :cid "methods/cid.py"} :ingest {:gated "G8 — bounded public slice runs freely (R1-authorized); scope expansion (more QIDs/sources) needs Council + operator DID" :source "Wikidata Special:EntityData (PUBLIC, CC0, no auth)" :individual_level false :manifest "data/ingest-sources.edn"} :tests {:count 30 :runner "python3 tests/test_simulate.py && python3 tests/test_distribution.py && python3 tests/test_runtime.py && python3 tests/test_ingest.py" :network false}}
